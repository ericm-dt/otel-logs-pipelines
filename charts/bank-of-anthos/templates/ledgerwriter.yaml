{{- if .Values.ledgerwriter.enabled }}
# ---------------------------------------------------------------------------
# ledgerwriter – Validates and writes new transactions to ledger (Java / Spring Boot)
# ---------------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledgerwriter
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "boa.selectorLabels" "ledgerwriter" | nindent 4 }}
spec:
  replicas: {{ .Values.ledgerwriter.replicas }}
  selector:
    matchLabels:
      {{- include "boa.selectorLabels" "ledgerwriter" | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "boa.selectorLabels" "ledgerwriter" | nindent 8 }}
      {{- $annotations := include "boa.podAnnotations" (list $.Values.global.podAnnotations .Values.ledgerwriter.podAnnotations) }}
      {{- if $annotations }}
      annotations:
        {{- $annotations | nindent 8 }}
      {{- end }}
    spec:
      terminationGracePeriodSeconds: 5
      containers:
        - name: ledgerwriter
          image: {{ include "boa.image" (list . .Values.ledgerwriter .Values.ledgerwriter.imageName) }}
          imagePullPolicy: {{ include "boa.imagePullPolicy" . }}
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: environment-config
            - configMapRef:
                name: service-api-config
            - configMapRef:
                name: ledger-db-config
          env:
            - name: PORT
              value: "8080"
            - name: VERSION
              value: {{ .Values.global.imageTag | quote }}
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: ENABLE_TRACING
              value: "false"
            - name: ENABLE_METRICS
              value: "false"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.ledgerDb.credentials.secretName }}
                  key: SPRING_DATASOURCE_PASSWORD
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 15
          volumeMounts:
            - name: jwt-key
              mountPath: /tmp/.ssh
              readOnly: true
          resources:
            {{- toYaml .Values.ledgerwriter.resources | nindent 12 }}
      volumes:
        {{- include "boa.jwtPublicKeyVolume" . | nindent 8 }}
---
apiVersion: v1
kind: Service
metadata:
  name: ledgerwriter
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    {{- include "boa.selectorLabels" "ledgerwriter" | nindent 4 }}
  ports:
    - port: 8080
      targetPort: 8080
{{- end }}
